// Frontend runtime configuration.
// Leave empty to use same-origin `/api` (works with local nginx proxy / Docker Compose).
// For separate deployments (static frontend + separate API host), set this to the API base URL:
// window.JD_API_BASE = "https://api.example.com";
// Supabase Edge Functions example:
// window.JD_API_BASE = "https://<project-ref>.supabase.co/functions/v1";

window.JD_API_BASE = "https://nhdsppxrxturjgtpfxjx.supabase.co/functions/v1";
